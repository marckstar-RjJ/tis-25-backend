    server {
        listen 80;
        server_name your_domain_or_render_url; # Replace with your domain or Render URL

        root /var/www/html/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000; # Connect to php-fpm service
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    # Optional: prevent access to .env file and other sensitive files
    location ~ /\.env {
        deny all;
    }

    location ~ /\.ht {
        deny all;
    }
}
```
4.  **Guarda el archivo `nginx.conf` en tu editor.** **¡Asegúrate de que el guardado se complete sin errores!**
5.  Abre tu terminal en `C:\proyecto_tis25\tis-25-backend`.
6.  **Ahora, usa un comando para mostrar el contenido del archivo directamente en la terminal:**
    ```powershell
    type nginx.conf
    ```
    (En PowerShell, `type` muestra el contenido de un archivo).
7.  **VERIFICA VISUALMENTE la salida del comando `type nginx.conf` en tu terminal.** La salida debe empezar con la línea `server {`. Si no empieza así, significa que el archivo no se guardó correctamente con el contenido completo.

**Si el comando `type nginx.conf` muestra que el archivo SÍ empieza con `server {` y tiene el contenido completo:**

Esto significa que el archivo local es correcto, pero Git no lo está detectando. En este caso, fuerza a Git a agregar el archivo aunque crea que no ha cambiado:

1.  ```bash
    git add nginx.conf